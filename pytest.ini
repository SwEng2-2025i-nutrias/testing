[pytest]
minversion = 6.0
addopts = -ra -q
testpaths = .
python_files = test_*.py *_test.py
markers =
    unit: pruebas unitarias de lógica pura
    integration: pruebas de integración que usan base de datos o servicios externos
