
minversion = 6.0
addopts = -ra -q  # -r a muestra skip/xfail, -q silencio excesos
testpaths = tests
python_files = test_*.py *_test.py
markers =
    integration: pruebas que tocan DB/external services
    unit: pruebas puras de lógica
